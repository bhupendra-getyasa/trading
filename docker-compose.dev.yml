services:

  redis:
    image: redis:7
    container_name: redis

    ports:
      - "6379:6379"

    volumes:
      - redis_data:/data

    command: redis-server --appendonly yes

  ingestion-service:
    build:
      context: .
      dockerfile: apps/ingestion-service/Dockerfile.dev

    container_name: ingestion-service

    ports:
      - "4001:4001"

    env_file:
      - .env

    volumes:
      - .:/app
      - node_modules:/app/node_modules

    working_dir: /app/apps/ingestion-service

    command: npx nodemon --config ../../nodemon.json  src/index.js --legacy-watch

    depends_on:
      - redis


  websocket-service:
    build:
      context: .
      dockerfile: apps/websocket-service/Dockerfile.dev

    container_name: websocket-service

    ports:
      - "4000:4000"

    env_file:
      - .env

    volumes:
      - .:/app
      - node_modules:/app/node_modules

    working_dir: /app/apps/websocket-service

    command: npx nodemon --config ../../nodemon.json  src/index.js --legacy-watch

    depends_on:
      - redis

  agent-service:
    build:
      context: .
      dockerfile: apps/agent-service/Dockerfile.dev

    container_name: agent-service

    ports:
      - "4002:4002"

    env_file:
      - .env

    volumes:
      - .:/app
      - node_modules:/app/node_modules

    working_dir: /app/apps/agent-service

    command: npx nodemon --config ../../nodemon.json  src/index.js --legacy-watch

    depends_on:
      - redis

volumes:
  node_modules:
  redis_data: