{
  "name": "trading",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "docker compose up --build",
    "down": "docker compose down"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
